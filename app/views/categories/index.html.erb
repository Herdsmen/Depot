<% if notice %>
	<p id="notice">
		<%= notice %>
	</p>
<% end %>

<div class="title">
	<span class="title_icon">
		<%= image_tag("bullet1.gif") %>
	</span>
	<%= t('.list_categories')%>
</div>

<div id="category">
<%= treeDisplay(@categoriesHashTree) %>
</div>
<br />
<%= button_to t('.new_category'), new_category_path, 
	{:method => :get, :class => "image_submit" } %>
