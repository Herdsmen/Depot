<div class="title">
	<span class="title_icon">
		<%= image_tag("bullet1.gif") %>
	</span>
	<%= t('.list_products')%>
</div>

<div class="feat_prod_box_details">
  <table>
  <% @products.each do |product| %>
    <tr class="<%= cycle("list_line_odd", "list_line_even") %>">

      <td>
        <%= image_tag(product.image_url, :width => 98) %>
      </td>

      <td class="list_description">
        <dl>
          <dt><%= product.title %></dt>
          <dd><%= truncate(strip_tags(product.description),
                 :length => 80) %></dd>
        </dl>
      </td>

      <td class="list_actions">
        <%= link_to t('.show'), product %><br/>
        <%= link_to t('.edit'), edit_product_path(product) %><br/>
        <%= link_to t('.destroy'), product, 
                    :confirm => t('.areyousure'),
                    :method => :delete %>
      </td>
    </tr>
  <% end %>
  </table>
  <p><%= will_paginate @products %></p>
</div>

<br />

<%= button_to t('.new_product'), new_product_path,
	{ :class => "image_submit", :method => :get } %>
